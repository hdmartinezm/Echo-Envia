# Node Environment
NODE_ENV=development
PORT=3000

# Azure Key Vault (se configura automáticamente en Azure)
KEY_VAULT_URL=https://kv-envia-dev.vault.azure.net/

# Database (se obtiene de Key Vault en producción)
DB_HOST=mysql-envia-dev.mysql.database.azure.com
DB_PORT=3306
DB_USER=enviaadmin
DB_PASSWORD=
DB_NAME=enviadb

# Azure Storage
STORAGE_ACCOUNT_NAME=stenviadev

# Application Insights (se configura automáticamente)
APPINSIGHTS_INSTRUMENTATIONKEY=
APPLICATIONINSIGHTS_CONNECTION_STRING=

# Azure AD (para Managed Identity)
AZURE_CLIENT_ID=
AZURE_TENANT_ID=
AZURE_CLIENT_SECRET=
